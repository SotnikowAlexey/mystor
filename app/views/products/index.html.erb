<table border="1" bordercolor="#000000">
<% @products.each do |e| %>
      <td align="center"><b><%= e.name.to_s %></b><br/><i> price: <%= e.price.to_s%></i><br/> 
        <form method="POST" action="/show"> 
          <button type="submit"><img src="<%=e.img.to_s%>"></button>
          <input name="name" value="<%= e.id %>" type="hidden">          
        </form>    
      <form method="POST" action="/cart">
        <button type="submit">add to cart</button>
        <input name="name" value="<%= e.name %>" type="hidden">
        <input name="price" value="<%= e.price %>" type="hidden">
      </form>
      </td>
<% end %>
 
  </table>
<a href="/cart">Go to Cart</a></div></body> </html>
